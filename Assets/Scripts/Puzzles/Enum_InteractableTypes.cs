using UnityEngine;

public class Enum_InteractableTypes : MonoBehaviour
{
    public enum InteractableType
    {
        Mushroom,
        Pinecone,
        Crate,
        Grass
    }

    public InteractableType type;
}
